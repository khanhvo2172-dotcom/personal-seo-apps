# --- Server stack: pinned to a known-good pair (do not loosen) ---
# Streamlit 1.61.0's gzip middleware subclasses Starlette's GZipResponder and
# calls it WITHOUT the `thread_minimum_size` argument. Starlette >= 1.4 made
# that argument required, which crashes the app on every request with:
#   TypeError: GZipResponder.__init__() missing 1 required keyword-only
#   argument: 'thread_minimum_size'
# starlette==1.3.1 is the last release compatible with Streamlit 1.61.0's call.
streamlit==1.61.0
starlette==1.3.1

# --- App dependencies ---
requests
pandas
python-dotenv
Pillow
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
gdown
cloudinary
openpyxl
streamlit-sortables
beautifulsoup4
